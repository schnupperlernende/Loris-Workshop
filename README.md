# Loris-Workshop

Start with NGINX Webserver task

Logged in in the other Computer with the Ip-Adresse for the command `ssh ubuntu@IP-Adresse`

Updated and upgradet System Packages with 

- `sudo apt update`
- `sudo apt upgrade -y`

Installed Nginx with command `sudo apt install nginx -y`

Skipped Step 2 (Adjusting the Firewall)

Checked if Web Server is running with the command `systemctl status nginx` and changed some text of html

Didn`t do the other Steps and finished with the how to Install Nginx on Ubuntu task

Installed docker with the commands 

- `sudo apt-get update`
- `sudo apt-get install docker.io`

Started docker with the command `sudo systemctl start docker`

Tried docker with the command `sudo docker run hello-world`
